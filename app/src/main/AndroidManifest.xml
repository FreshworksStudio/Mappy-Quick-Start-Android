<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.mappy.mappysdkimplementationexample">

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MappySDKImplementationExample" >

        <activity
            android:name=".InitialActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".kotlin.activities.VenuesActivityKt"
            android:exported="false" />

        <activity
            android:name=".kotlin.activities.MapActivityKt"
            android:exported="false" />

        <activity
            android:name=".java.activities.VenuesActivity"
            android:exported="false" />
        <activity
            android:name=".java.activities.MapActivity"
            android:exported="false" />

    </application>

</manifest>